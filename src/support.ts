/**
 * Public support address. Static legal pages cannot import this module, so the
 * production-readiness check verifies their mailto links stay in sync.
 */
export const SUPPORT_EMAIL = 'hello@hemu.krd';
